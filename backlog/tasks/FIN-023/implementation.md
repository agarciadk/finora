# Implementation

## Approach

Cannot be finalized yet — depends on the bounds/replace-vs-supplement decision in `specification.md`. If pursued, would likely pair a slider with the existing numeric `Input` (kept in sync) rather than fully replacing it, to preserve exact-amount entry.

## Files / Areas Affected

- `apps/web/src/components/` recurring payment form.

## Implementation Steps

- [ ] Resolve range bounds/behavior in `specification.md` first.

## Testing

- [ ] N/A until scope is decided.

## Validation

- [ ] N/A until scope is decided.

## Discovered Work (out of scope)

—

## Notes

Low priority — the inbox itself frames this as a tentative suggestion ("podría ser"), not a firm requirement.
