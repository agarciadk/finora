# Implementation

## Approach

Cannot be determined until a name is chosen and the blast radius (UI-only vs. repo/package/domain rename) is decided.

## Files / Areas Affected

- Potentially very broad: `package.json` names, `apps/web/src/i18n/`, `README.md`, `.ai-context/`, `render.yaml`, `apps/web/vercel.json`, repository name, deployed domains.

## Implementation Steps

- [ ] Resolve the open questions in `specification.md` first.

## Testing

- [ ] N/A until scope is decided.

## Validation

- [ ] N/A until scope is decided.

## Discovered Work (out of scope)

—

## Notes

Kept in `refinement` — a rename without a chosen name isn't actionable.
