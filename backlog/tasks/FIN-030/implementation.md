# Implementation

## Approach

Cannot be finalized yet — depends entirely on the technology decision in `specification.md`.

## Files / Areas Affected

- Likely a new `apps/mobile` (or similar) workspace package if Capacitor/React Native is chosen; changes to `apps/web/vite.config.ts`/`public/` if a PWA approach is chosen.

## Implementation Steps

- [ ] Resolve the technology choice in `specification.md` first (ideally after FIN-029's audit).

## Testing

- [ ] N/A until scope is decided.

## Validation

- [ ] N/A until scope is decided.

## Discovered Work (out of scope)

—

## Notes

Largest, least-defined mobile idea — kept in `refinement` at low priority until FIN-029 and a technology decision exist.
