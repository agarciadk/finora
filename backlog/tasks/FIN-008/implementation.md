# Implementation

## Approach

Cannot be determined — this depends entirely on provider/vendor selection, which is a business decision not yet made anywhere in the repo.

## Files / Areas Affected

- Likely a new backend module (`apps/api/src/`), new Prisma models for linked institutions/connections, and new frontend flows — exact shape unknown until the provider is chosen.

## Implementation Steps

- [ ] Resolve provider/consent-flow/sync-model decisions in `specification.md` first.

## Testing

- [ ] N/A until scope is decided.

## Validation

- [ ] N/A until scope is decided.

## Discovered Work (out of scope)

—

## Notes

This is the largest, least-defined idea in the inbox. Kept in `refinement` deliberately; do not let this become `ready` without an explicit provider/vendor decision.
