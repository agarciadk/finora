# Implementation

## Approach

Cannot be determined — no design direction exists yet. Likely touches Tailwind theme tokens/CSS variables in `apps/web/src/globals.css` once a direction is chosen.

## Files / Areas Affected

- `apps/web/src/globals.css` (design tokens/CSS variables).
- Broad UI surface, given it's a palette/typography change.

## Implementation Steps

- [ ] Establish a concrete design direction (palette, font, background) before any implementation.

## Testing

- [ ] N/A until scope is decided.

## Validation

- [ ] N/A until scope is decided.

## Discovered Work (out of scope)

—

## Notes

Kept in `refinement` — implementing this without a design direction would mean inventing product decisions.
