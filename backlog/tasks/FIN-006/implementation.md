# Implementation

## Approach

Cannot be finalized yet — depends on the layout decisions in `specification.md`. Likely touches `apps/web/src/components/dashboard-layout.tsx` (header contents), `apps/web/src/components/app-sidebar.tsx` (new footer/menu section), and the Settings page's Profile tab (`apps/web/src/pages` / `components/settings`).

## Files / Areas Affected

- `apps/web/src/components/dashboard-layout.tsx`
- `apps/web/src/components/app-sidebar.tsx`
- Settings/Profile tab components.

## Implementation Steps

- [ ] Resolve the open questions in `specification.md` first.

## Testing

- [ ] N/A until scope is decided.

## Validation

- [ ] N/A until scope is decided.

## Discovered Work (out of scope)

—

## Notes

Kept in `refinement` — this is a navigation/UX redesign without a defined target layout yet.
